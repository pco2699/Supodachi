# Require

- yarn
  - `npm install -g yarn`
  - `brew install yarn`
  - どちらでも

# Build Setup

- 初期化

`yarn install`

- サーバー起動
  - 仕様書の確認
    - `yarn run api:preview`
    - http://127.0.0.1:3010/
  - mock
    - `yarn run api:mock`
    - http://127.0.0.1:3011/